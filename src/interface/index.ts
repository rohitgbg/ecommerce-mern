export interface ISliderItem {
    id: number,
    img: string,
    title: string,
    desc: string,
    bg: string,
}

export interface Category  {
    id: number;
    img: string;
    title: string;
}